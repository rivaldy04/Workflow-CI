name: train_earthquake_model

conda_env: conda.yaml

entry_points:
  main:
    parameters:
      n_estimators:
        type: int
        default: 300
      max_depth:
        type: int
        default: 20
    command: "python modelling.py --n_estimators {n_estimators} --max_depth {max_depth}"
